# สรุปโปรเจกต์: ระบบให้คะแนนข้อเสนอหัวข้อ (Proposal) — วิชาปัญหาพิเศษ

## สถานะปัจจุบัน
ใช้งานได้แล้วจริง — Deploy เป็น 2 ส่วน:
- **Backend/API + Database:** Google Apps Script + Google Sheets
  (deployment URL: `https://script.google.com/macros/s/AKfycbzYTvGLDpeLlW6FqIl1KX1tpOcatmjrnkKiUYL1jfx7tCHs8jPmX8Zm2wqMJsfR11Vm/exec`)
- **Frontend:** static `index.html` บน GitHub Pages เรียก Apps Script ผ่าน `fetch()`

## โครงสร้างไฟล์
```
proposal-scoring-webapp/
├── Code.gs              # Apps Script backend (doGet/doPost เป็น JSON API + ฟังก์ชัน setup)
├── Index.html           # หน้าเว็บสำรอง เปิดตรงจาก Apps Script URL (ใช้ google.script.run)
├── github-pages/
│   └── index.html       # หน้าเว็บหลักที่กรรมการใช้จริง (ใช้ fetch แทน google.script.run)
└── คู่มือติดตั้ง.md      # ขั้นตอน deploy ทั้งสองฝั่ง
```

## Google Sheet — โครงสร้างข้อมูล
- **Students** (6 คอลัมน์): รหัสนิสิต, ชื่อ-สกุล, ห้องสอบ, แผนการเรียน, อาจารย์ที่ปรึกษา, หัวข้อปัญหาพิเศษ
  — มีข้อมูลจริง 51 คน (ปกติ 21 คน: SC11-103×12 + SC11-203×9 / พิเศษ 30 คน: SC11-103×18 + SC11-203×12)
- **Committees** (3 คอลัมน์): แผนการเรียน, ห้องสอบ, ชื่อกรรมการ — กรรมการ 3 ท่านคงที่ต่อห้องทั้งวัน
  (ปกติ SC11-103 = กนิษฐา/ปัญญาพร/สุริยะ, ปกติ SC11-203 = โรจนี/ธีรานันต์/กรานตวัน,
   พิเศษ SC11-103 = ชุดเดียวกับปกติ, พิเศษ SC11-203 = ธีรานันต์/โรจนี/กรานตวัน)
- **Scores**: Timestamp, รหัสนิสิต, ชื่อนิสิต, ห้องสอบ, แผนการเรียน, ชื่อกรรมการ, คะแนนดิบ 5 เกณฑ์ (0-4), คะแนนรวม (เต็ม 10)
  — เป็น upsert (กรรมการเดิม+นิสิตเดิม กรอกซ้ำจะเขียนทับแถวเดิม)
- **Summary**: รหัสนิสิต, ชื่อ, จำนวนกรรมการที่ประเมินแล้ว, คะแนนเฉลี่ย — คำนวณด้วยสูตร `AVERAGEIF` อัตโนมัติ

## เกณฑ์การให้คะแนน (หมวด "ข้อเสนอหัวข้อ" 10 คะแนน จาก 4 หมวดของทั้งวิชา 10:50:20:20)
มาตรวัดกลาง 0-4 ทุกเกณฑ์ (ไม่ผ่าน/ต้องปรับปรุง/พอใช้/ดี/ดีเยี่ยม) แปลงเป็นคะแนนจริงด้วย
`(คะแนนดิบ/4) × น้ำหนัก`:

| เกณฑ์ | น้ำหนัก |
|---|---|
| ความชัดเจนของปัญหาและวัตถุประสงค์ | 3 |
| ความเหมาะสมของขอบเขตงาน | 3 |
| ความรู้พื้นฐาน/ทบทวนเทคโนโลยีที่เกี่ยวข้อง | 2 |
| ความเป็นประโยชน์/กลุ่มผู้ใช้ที่ชัดเจน | 1 |
| การนำเสนอและตอบข้อซักถาม | 1 |

**⚠️ ค่านี้ถูกกำหนดซ้ำ 2 ที่:** ใน `Code.gs` (ตัวแปร `CRITERIA`) และใน
`github-pages/index.html` (ตัวแปร `CRITERIA` เหมือนกัน) เพราะไฟล์แยกกันคนละที่โฮสต์
แก้ต้องแก้ทั้งคู่ให้ตรงกัน

## จับคู่กับ CLO/PLO (สำหรับ SAR/AUN-QA)
หมวด "ข้อเสนอหัวข้อ" วัด **CLO2 → PLO2** (เลือกใช้เทคโนโลยีเหมาะสม → ออกแบบฐานข้อมูล/เขียนโปรแกรมอย่างเป็นระบบ)
CLO/PLO อื่น (1,3,4,5 ↔ 1,3,4,5 ตามลำดับ) ยังไม่ได้ทำ rubric ละเอียด รอทำหมวด 2/3/4 ทีหลัง

## Flow การใช้งานของกรรมการ
1. เลือกชื่อกรรมการครั้งเดียว (จำใน `localStorage`)
2. พิมพ์รหัสนิสิต → auto-fill ชื่อ/ห้อง/แผนการเรียน/ที่ปรึกษา/หัวข้อ
3. ถ้ารหัสไม่ตรงห้องที่กรรมการประจำ → เตือนสีแดง (ไม่บล็อก)
4. ให้คะแนน 5 เกณฑ์ → บันทึก → ฟอร์มเคลียร์พร้อมรับคนถัดไป

## ข้อควรระวังทางเทคนิค (สำคัญถ้าจะแก้ต่อ)
- **CORS:** fetch POST ไป Apps Script ต้อง**ห้ามตั้ง** `Content-Type: application/json`
  (ปล่อยเป็น text/plain ค่าเริ่มต้น) ไม่งั้น browser จะส่ง preflight OPTIONS ที่ Apps Script ตอบไม่ได้
- Apps Script ต้อง deploy แบบ **"Who has access: Anyone"** (ไม่ใช่ "Anyone with Google account")
  ไม่งั้นกรรมการต้อง login ก่อนถึงเรียก API ได้ — หมายความว่า**ไม่มีการยืนยันตัวตนใดๆ**
  ใครมีลิงก์ก็เรียก API ได้ (ยอมรับความเสี่ยงนี้ไว้เพราะใช้แค่วันเดียว)
- ห้ามรัน `initializeStudents` / `initializeCommittees` ซ้ำหลังแก้ข้อมูลในชีทด้วยมือแล้ว
  (จะเขียนทับข้อมูลที่แก้ไว้)
- แก้ `Code.gs` แล้วต้อง **Deploy → Manage deployments → New version** ใหม่ทุกครั้ง

## กำลังคิดจะ redesign ใหม่
ยังไม่ได้บอกรายละเอียดว่าจะ redesign ส่วนไหน (UI, สถาปัตยกรรม, เพิ่มหมวดคะแนนอื่น ฯลฯ) —
ถ้าเล่าให้ Claude Code ฟังต่อว่าอยากเปลี่ยนอะไร จะไปต่อจากจุดนี้ได้เลย
