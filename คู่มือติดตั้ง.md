# คู่มือติดตั้งระบบให้คะแนนข้อเสนอหัวข้อ (Proposal) — Google Apps Script

ระบบนี้ให้กรรมการแต่ละท่านกรอกคะแนนจากมือถือ/แท็บเล็ตของตัวเอง ข้อมูลจะถูกบันทึกลง
Google Sheet และมีชีท **Summary** ที่คำนวณคะแนนเฉลี่ยของนิสิตแต่ละคนให้อัตโนมัติ
(ทันทีที่มีกรรมการกรอกครบ จะเห็นค่าเฉลี่ยขึ้นเองโดยไม่ต้องกดคำนวณ)

**วิธีใช้งานของกรรมการ (เร็วที่สุด):**
1. เปิดเว็บ → เลือกชื่อตัวเอง 1 ครั้ง (ระบบจำไว้ในเบราว์เซอร์เครื่องนั้น ไม่ต้องเลือกซ้ำ)
2. พิมพ์ **รหัสนิสิต** → ระบบขึ้นชื่อ, ห้องสอบ, แผนการเรียน, ที่ปรึกษา, และ**หัวข้อปัญหาพิเศษ**
   ให้อัตโนมัติทันที ไม่ต้องกรอกเอง
3. กดให้คะแนน 5 เกณฑ์ → กดบันทึก → ฟอร์มเคลียร์ตัวเองพร้อมรับนิสิตคนถัดไปทันที

## ขั้นตอนที่ 1: สร้าง Google Sheet + เปิด Apps Script

1. ไปที่ [sheets.google.com](https://sheets.google.com) สร้างไฟล์ใหม่ ตั้งชื่อ เช่น
   `คะแนนข้อเสนอหัวข้อ ปัญหาพิเศษ 2569`
2. เมนู **ส่วนขยาย (Extensions) → Apps Script**
3. จะเห็นไฟล์ `Code.gs` เปล่าๆ อยู่แล้ว — ลบโค้ดเดิมทั้งหมดออก แล้ววางเนื้อหาจากไฟล์
   `Code.gs` ที่แนบมาให้แทน
4. คลิกเครื่องหมาย `+` ข้าง "ไฟล์" (Files) เลือก **HTML** ตั้งชื่อไฟล์ว่า `Index`
   (สำคัญ: ต้องชื่อ `Index` ให้ตรงตัวพิมพ์ใหญ่-เล็ก) แล้ววางเนื้อหาจากไฟล์
   `Index.html` ที่แนบมาให้
5. กด **บันทึก** (ไอคอนรูปแผ่นดิสก์ หรือ Ctrl+S)

## ขั้นตอนที่ 2: สร้างชีทและนำเข้ารายชื่อนิสิต

1. ที่แถบด้านบนของ Apps Script editor เลือกฟังก์ชัน `setupSheets` จาก dropdown
   (อยู่ข้างปุ่ม ▷ Run) แล้วกด **Run**
   - ครั้งแรกจะมีหน้าต่างขอสิทธิ์ (Authorization) ให้กด "ตรวจสอบสิทธิ์" (Review permissions)
     เลือกบัญชี Google ของตัวเอง แล้วกด "อนุญาต" (Allow) — เป็นขั้นตอนปกติของ Apps Script
   - จะเห็น popup แจ้งว่าสร้างชีท Students, Committees, Scores, Summary เรียบร้อย
2. เลือกฟังก์ชัน `initializeStudents` จาก dropdown แล้วกด **Run**
   - รายชื่อนิสิต 51 คน (ปกติ 21 คน + พิเศษ 30 คน ทั้ง 2 ห้องสอบ) พร้อม **หัวข้อปัญหาพิเศษ**
     ของแต่ละคน
     จากตารางที่เคยจัดทำไว้จะถูกนำเข้าอัตโนมัติ
   - ถ้ามีรายชื่อ/หัวข้อผิด ไปแก้ไขตรงๆ ในชีท **Students** ได้เลย (ไม่ต้องรันสคริปต์ซ้ำ —
     ห้ามรัน `initializeStudents` ซ้ำอีกเพราะจะเขียนทับข้อมูลที่แก้ไว้)
3. เลือกฟังก์ชัน `initializeCommittees` จาก dropdown แล้วกด **Run**
   - นำเข้ารายชื่อกรรมการ 3 ท่านต่อห้อง/แผนการเรียน (คงที่ทั้งวัน) ตามที่สรุปกันไว้
   - แก้ไขรายชื่อกรรมการได้ตรงๆ ในชีท **Committees** ภายหลัง (เช่น สลับตัวกรรมการ) โดยไม่ต้อง
     รันสคริปต์ซ้ำ
   - ถ้าต้องการเพิ่ม/แก้รายชื่อนิสิต แล้วอยากให้สูตรใน Summary ครอบคลุมแถวใหม่ ให้รันฟังก์ชัน
     `buildSummaryFormulas` อีกครั้ง

## ขั้นตอนที่ 3: Deploy Apps Script เป็น API (สำหรับให้ GitHub Pages เรียกใช้)

เวอร์ชันนี้แยกเป็น 2 ส่วน: **Apps Script ทำหน้าที่เป็น API + ฐานข้อมูล** (Google Sheet) ส่วน
**หน้าเว็บจริงที่กรรมการเปิดใช้ ให้โฮสต์บน GitHub Pages** (เหมือนระบบสหกิจที่เคยทำไว้ก่อนหน้านี้)

1. มุมขวาบนของ Apps Script editor กด **Deploy → New deployment**
2. ตรงช่อง "Select type" กดไอคอนเฟือง เลือก **Web app**
3. ตั้งค่า:
   - **Execute as:** Me (บัญชีของคุณ)
   - **Who has access:** เลือก **"Anyone"** (จำเป็นสำหรับเวอร์ชันนี้ เพราะ GitHub Pages
     เรียกจากเบราว์เซอร์ของกรรมการโดยตรง ไม่ผ่านการ login ด้วย Google account — ถ้าเลือก
     "Anyone with Google account" กรรมการจะต้อง login ก่อนถึงจะเรียก API ได้ ซึ่งจะทำให้
     fetch ล้มเหลวเงียบๆ)
4. กด **Deploy** → คัดลอกลิงก์ที่ได้ (รูปแบบ
   `https://script.google.com/macros/s/xxxxxxxxxxxx/exec`)

> **ถ้าแก้โค้ด `Code.gs` ภายหลัง** ต้องกด **Deploy → Manage deployments → แก้ไข (ไอคอนดินสอ)
> → New version → Deploy** ใหม่ทุกครั้ง ไม่เช่นนั้นลิงก์เดิมจะยังใช้โค้ดเวอร์ชันเก่าอยู่
> (URL จะไม่เปลี่ยนถ้าใช้ deployment เดิม ไม่ต้องแก้ `GAS_URL` ในหน้าเว็บซ้ำ)

> **หมายเหตุด้านความปลอดภัย:** เพราะตั้งเป็น "Anyone" ใครก็ตามที่มีลิงก์นี้สามารถเรียก API
> อ่าน/เขียนข้อมูลได้โดยตรง (ไม่มีการยืนยันตัวตน) เหมาะสำหรับงานที่ใช้ภายในช่วงเวลาสั้นๆ
> อย่างการสอบหัวข้อ 1 วัน แต่ไม่ควรใช้กับข้อมูลที่ละเอียดอ่อนกว่านี้ ถ้ากังวลเรื่องนี้
> แจ้งได้ ผมจะเพิ่มการเช็ค secret token ง่ายๆ ในโค้ดให้

## ขั้นตอนที่ 4: Deploy หน้าเว็บบน GitHub Pages

1. ในไฟล์ `github-pages/index.html` ที่แนบมาให้ แก้บรรทัด:
   ```js
   const GAS_URL = 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec';
   ```
   เปลี่ยน `YOUR_DEPLOYMENT_ID/exec` เป็นลิงก์จริงที่ได้จากขั้นตอนที่ 3
2. สร้าง repository ใหม่บน GitHub (หรือใช้ repo เดิมที่เคยทำระบบสหกิจ) แล้วอัปโหลดไฟล์
   `index.html` นี้ไว้ที่ root ของ repo (หรือโฟลเดอร์ `docs/` ตามที่ตั้งค่า Pages ไว้)
3. ไปที่ **Settings → Pages** ของ repo ตั้งค่า Source เป็น branch และโฟลเดอร์ที่เก็บไฟล์
4. รอสักครู่ จะได้ลิงก์ประมาณ `https://<username>.github.io/<repo>/`
5. ส่งลิงก์นี้ให้กรรมการเปิดจากมือถือ/แท็บเล็ตในวันสอบ

> **ถ้าแก้เกณฑ์คะแนนภายหลัง (น้ำหนัก/จำนวนเกณฑ์):** ต้องแก้ 2 ที่ให้ตรงกัน — `CRITERIA` ใน
> `Code.gs` (ฝั่ง Apps Script) และ `CRITERIA` ใน `github-pages/index.html` (ฝั่งหน้าเว็บ)
> เพราะไฟล์ทั้งสองไม่ได้เชื่อมกันโดยตรงอีกต่อไปเมื่อแยกไปคนละที่โฮสต์



## วิธีใช้งานหน้าเว็บ (สำหรับกรรมการ)

**ทำครั้งเดียวตอนเปิดเว็บ:**
1. เลือกชื่อตัวเองจาก dropdown (มีเฉพาะกรรมการที่กำหนดไว้ในชีท Committees + ตัวเลือก
   "อื่นๆ" เผื่อมีอาจารย์ท่านอื่นนั่งแทน) — ระบบจะจำชื่อไว้ในเบราว์เซอร์เครื่องนั้น ไม่ต้องเลือกซ้ำ
   แม้ปิดเปิดเว็บใหม่

**ทำซ้ำทุกครั้งที่มีนิสิตคนใหม่:**
2. พิมพ์ **รหัสนิสิต** ในช่องเดียว — ระบบจะขึ้นชื่อ-สกุล, ห้องสอบ, แผนการเรียน, อาจารย์ที่ปรึกษา
   และ**หัวข้อปัญหาพิเศษ** ให้อัตโนมัติทันที ไม่ต้องกรอกอะไรเพิ่ม
   - ถ้ารหัสนิสิตไม่ตรงกับห้อง/แผนการเรียนที่กรรมการท่านนี้ประจำอยู่ (ตามชีท Committees)
     จะขึ้นข้อความเตือนสีแดงให้ตรวจสอบก่อน (แต่ยังกดบันทึกได้ เผื่อกรณีมีเหตุผลจริง เช่น
     กรรมการนั่งแทนข้ามห้อง)
3. ให้คะแนนแต่ละเกณฑ์ (5 เกณฑ์ ระดับ 0-4) ระบบคำนวณคะแนนรวมเต็ม 10 ให้อัตโนมัติ
4. กด **บันทึกคะแนน** → ฟอร์มเคลียร์ตัวเอง พร้อมรับนิสิตคนถัดไปทันที (ไม่ต้องเลือกชื่อกรรมการซ้ำ)
5. ถ้ากรอกผิดแล้วอยากแก้ไข ให้พิมพ์รหัสนิสิตคนเดิมซ้ำ (กรรมการคนเดิม) แล้วให้คะแนนใหม่ + กดบันทึก
   ระบบจะเขียนทับคะแนนเดิมโดยอัตโนมัติ (ไม่สร้างแถวซ้ำ)
6. ถ้าต้องการสลับเป็นอีกท่าน (เช่น ใช้เครื่องเดียวกันหลายคน) กดลิงก์ **"เปลี่ยนกรรมการ"**
   มุมบนของการ์ดกรอกคะแนน

## ผลลัพธ์

- ชีท **Scores** เก็บคะแนนดิบทุกครั้งที่มีการบันทึก (1 แถวต่อกรรมการ 1 ท่าน ต่อ นิสิต 1 คน)
  พร้อมห้องสอบ/แผนการเรียนกำกับไว้ในแต่ละแถวเพื่อตรวจสอบย้อนหลังได้
- ชีท **Summary** แสดง:
  - จำนวนกรรมการที่ให้คะแนนนิสิตแต่ละคนแล้ว (ควรครบ 3 ท่านต่อคน)
  - คะแนนเฉลี่ยเต็ม 10 ของนิสิตแต่ละคน (คำนวณจากสูตร `AVERAGEIF` อัตโนมัติ ไม่ต้องกดอะไรเพิ่ม)

## ข้อควรระวัง

- ฟังก์ชัน `initializeStudents` และ `initializeCommittees` เขียนทับข้อมูลในชีทที่เกี่ยวข้องตั้งแต่
  แถวที่ 2 เป็นต้นไป **ห้ามรันซ้ำ** หลังจากที่แก้ไขข้อมูลในชีทด้วยมือแล้ว
- ชื่อกรรมการต้องสะกดตรงกันทุกครั้งสำหรับนิสิตคนเดียวกัน (ระบบ trim ช่องว่างหน้า-หลังให้แล้ว
  แต่ตัวสะกดต้องตรงกัน) — การเลือกจาก dropdown แทนการพิมพ์เองจะช่วยลดปัญหานี้ได้เกือบทั้งหมด
- ถ้าลบข้อมูลในเบราว์เซอร์ (clear browser data) ระบบจะลืมชื่อกรรมการที่เคยเลือกไว้ ต้องเลือกใหม่
